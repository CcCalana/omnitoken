# REVIEW Archive — Phase 2-A 收官 至 Phase 2-B 全程 (R-008 ~ R-005b-fix)

> 归档时间: 2026-05-19
> 范围: Phase 2-A (T-013/T-014) + Phase 2-B (T-005a/T-015/T-017a/T-005b) 全部 review 原文
> 最新 review 见根目录 `REVIEW.md`；R-001~R-007 见 `archive.md`；R-006* / R-006d 见 `archive-2026-05-12.md`

---

## R-008 (T-008, commits `4761671` + `e7f8036`)

**结论: `[+] Approved`** — 0C/0H/2M(info)/4N。覆盖率 93.7%。

**核心**: usage middleware 完全解耦 proxy，`context.Background()` deferred goroutine，三表事务原子写入，DB 端 numeric 精确计算，pricing CTE 三级 fallback。PROPOSAL 6 节全部精确落地。

**M-16**: body double-read (Phase 2 优化) | **M-17**: SQL fallback 双重防御 (设计正确)
**N-25~N-28**: README 补充 / SQL 注释 / capture WriteHeader / e2e cleanup — 均不阻塞

> 完整 review 见 R-008 归档前原文，关键表格保留：

| PROPOSAL 节 | 落地 | 接受标准 | 状态 |
|------------|------|---------|------|
| §1 middleware | ✅ | 解析点 non-stream/stream | ✅ |
| §2 包结构 | ✅ | 入账字段 events+breakdown | ✅ |
| §3 deferred goroutine | ✅ | cost_ledger pricing | ✅ |
| §4 numeric SQL | ✅ | 缺 pricing → cost=0+failed | ✅ |
| §5 failed zero-cost | ✅ | 同步入账不阻塞 | ✅ |
| §6 seed 价格 | ✅ | 失败不影响客户端 | ✅ |

---

## R-009a (T-009a, commits `ce204b5` + `d3eb0e6`)

**结论: `[+] Approved`** — 0C/0H/0M/1N。覆盖率 60.9%。

**正面信号 (10条)**:
1. ✅ `overviewStore` 接口扩展 `LoadUsers` + `LoadModels`，零破坏性变更
2. ✅ users SQL 精确：`users LEFT JOIN usage_events LEFT JOIN usage_token_breakdown`，当月窗口 `$1/$2`
3. ✅ models SQL 三级 COALESCE：`model_actual → model_requested → 'unknown'`，与 overview 一致
4. ✅ models SQL 包含 `cost_ledger` JOIN，前端可展示真实成本
5. ✅ handler pattern 与 overview 完全一致：nil store → 200 空数组、DB error → 500 + envelope
6. ✅ `emptyUsersResponse()` / `emptyModelsResponse()` 返回非 nil 空切片（JSON `[]` 不是 `null`）
7. ✅ users `quota` 字段固定 0，符合 "Demo 阶段无配额系统" 约束
8. ✅ 测试覆盖 8 case：handler 正常/空/error × 2 API + store SQL 映射 + 空结果 × 2 API
9. ✅ fakeOverviewStore 扩展干净，每个方法独立 called/now/err 字段
10. ✅ SQL 参数断言 + query 文本断言双验证

**N-33**: users query 的 `ORDER BY used_tokens DESC` 中 `used_tokens` 是 `SUM(utb.total_tokens)` 的别名，部分 Postgres 版本可能不支持在 ORDER BY 中使用 SELECT 别名。实际 PG 12+ 都支持，不阻塞。

---

## R-010 (T-010, commit `760020f`)

**结论: `[+] Approved`** — 0C/0H/1M(info)/2N。覆盖率 cmd/admin 63.2%（+11.3pt vs T-009a 后基线）。

**正面信号 (9条)**:
1. ✅ `adminAuthMiddleware` 抽离干净，overview/users/models/dev-vkey 四个 endpoint 复用同一把鉴权，没有副本
2. ✅ `subtle.ConstantTimeCompare` 替换 `==`，等长校验 + trim 前置，timing-safe 落地（顺手解了原 T-005c 一半范围）
3. ✅ 未配置 token 走 503 `admin_auth_not_configured` 而不是默认放行——满足"安全失效闭门"原则
4. ✅ `/healthz` 显式不挂 middleware，保留 LB/容器探活通道
5. ✅ Bearer 解析用 `strings.CutPrefix`，对 "Bearer " / "Bearer  " / "Bearer\t" 各种边界都安全
6. ✅ `writeAdminAuthNotConfigured` 复用 `errorEnvelope`，与 gateway 401 / overview 500 同 envelope 形状
7. ✅ 测试同时覆盖路由级（三条 read route 都 401）和 middleware 级（4 case 真值表），双层断言
8. ✅ `makeCreateVirtualKeyHandler` 签名瘦身（去掉 bootstrapToken 参数），鉴权统一从 middleware 进入
9. ✅ 503 错误码 `admin_auth_not_configured` 易识别，运维 grep 友好

**M-19** (informational): 503 在生产部署中代表"配置错误"，应配 alert，不阻塞，登记在 Phase 2 alerting 任务里。

**N-34**: 中间件签名 `func(http.Handler) http.Handler` 是 net/http 经典风格，OK；如果以后引入 chi router 可换 `chi.Middleware` 别名。
**N-35**: `bootstrapToken = strings.TrimSpace(bootstrapToken)` 闭包捕获后值不再变，文档化或加注释能避免误读，不阻塞。

### 接受标准（全部 ✅）

| 标准 | 状态 |
|------|------|
| 抽取 `adminAuthMiddleware` | ✅ |
| overview/users/models 全部走 middleware | ✅ |
| `/healthz` 不走鉴权 | ✅ |
| 401 统一 envelope | ✅ |
| 未配置 token → 503 不默认放行 | ✅ |
| 4 case 单测 | ✅ |
| 不做完整 RBAC | ✅ |

**Pre-push gate 进度: 3/4**

---

## R-012 (T-012, commit `d7e78e8`)

**结论: `[+] Approved`** — 0C/0H/0M/3N。cmd/loadtest 覆盖率 73.6%，548 行（main 353 + test 195）。

**正面信号 (10条)**:
1. ✅ 纯标准库 `sync.WaitGroup` + `chan`，无第三方依赖（符合任务约束）
2. ✅ `MAX_REQUESTS` 默认 100，`parseConfig` 在解析阶段就拒绝超限——成本控制比"跑时硬停"更安全
3. ✅ `-admin-token` 与 `OMNITOKEN_ADMIN_BOOTSTRAP_TOKEN` 双通道，与 T-010 admin 鉴权天然兼容（前后任务衔接干净）
4. ✅ 延迟统计含 P95 计算：`percentileLatency` 用 `sort.Slice` + `math.Ceil` 索引，比 hardcoded P50/P99 健壮
5. ✅ `requestResult` 拆 2xx/4xx/5xx/timeout/error 五桶，能区分网络问题 vs 业务错误
6. ✅ httptest server 覆盖 request fanout / overview 校验 / MAX 限 / 非 2xx / usage 断言失败 5 类场景，避免本地真方舟跑
7. ✅ `normalizedBaseURL` 容忍尾斜杠 / scheme 大小写——loadtest 工具用错地址不会神秘 hang
8. ✅ 不改 gateway/admin/usage 任何代码（接受标准底线）
9. ✅ admin overview 校验阶段断言 `total_tokens > 0` 且 `active_users >= 1`，"压完没数据"的回归会被立即抓住
10. ✅ README 一行点明用途和成本边界，不冗长

**N-36**: `defaultModel = "ark-code-latest"` 写死，未来多 provider 后需要按 virtual model 取——但当前 Phase 1 这样最快。
**N-37**: `isTimeout(err)` 用 `errors.Is` + `net.Error` 双检测，OK；可以补一条 fallback 检查 `errors.Is(err, context.DeadlineExceeded)` 更严密，nit only。
**N-38**: 工具未对真方舟跑过 100 请求（Codex 自承"避免本地 token 成本消耗"）——可接受，但 push 前我们要在 secrets 安全的 CI 上跑一次至少 10 请求的烟测，验收门由首次 push 后的 nightly 接管。

### 接受标准（全部 ✅）

| 标准 | 状态 |
|------|------|
| 新增 `cmd/loadtest/main.go` 标准库实现 | ✅ |
| 全参数支持（concurrency/requests/gateway/admin/key） | ✅ |
| 2xx/4xx/5xx/timeout 统计 | ✅ |
| overview 断言 tokens > 0 / users ≥ 1 | ✅ |
| 平均延迟 + P95 + 成功率汇总 | ✅ |
| `-race` 编译通过（Docker 已验证） | ✅ |
| `MAX_REQUESTS=100` 硬停 | ✅ |
| 不改 gateway/admin/usage / 不引第三方依赖 | ✅ |

**Pre-push gate 进度: 4/4 ✅ — 可以 push first commit batch**

---

## R-013-prop (T-013 PROPOSAL, commit `f444ba2`)

**结论: `[+] Approved with 3 questions / 4 nits`** — 可进入实施阶段，Q-1 是落地时必须复核的 invariant，Q-2/Q-3 仅需 Codex 在实施时显式确认即可，不再来一轮 propose。

### 正面信号 (10条)

1. ✅ **发现现有占位 schema** — `000001_init.up.sql:125` 已有 `audit_logs` 占位表（`actor / ip_address / before_state / after_state`）。Codex 没有重新建表，而是 `000007_admin_audit_logs.up/down.sql` rename + add 演进。grep 确认无 Go 代码引用旧列名，rename 无破坏性。
2. ✅ **JSONB + 双引号列名** — `"before"` / `"after"` 显式加双引号，避开 PG 内部 trigger 上下文中的语法保留歧义，工程严谨度高。
3. ✅ **CHECK 约束三连** — `actor_type IN ('bootstrap','user','system')` + `action <> ''` + `resource_type <> ''`，让脏数据在 DB 层 fail-loud，不靠应用层兜底。
4. ✅ **`status_code` 额外字段** — 比我任务卡多了这个字段，能区分"成功写"和"失败写尝试"（如 400 invalid body / 500 DB error）。不是过度设计，是补真实审计缺口；接受。
5. ✅ **索引白名单 + 黑名单各两条** — 主索引 `(actor_id, created_at DESC)` / `(resource_type, resource_id, created_at DESC)`；显式列出**不加**的 `action / status_code / request_id / user_agent / JSONB GIN` + 理由（"索引爆炸"），这是 PROPOSAL 应有的克制。
6. ✅ **Plan A handler SetBefore 取舍干净** — A 精确无 DB 开销 / B 高耦合且多 query / C 损失证据。结论 + 三句话理由比堆 1000 字论证更可信。
7. ✅ **失败容忍的安全口径** — "**鉴权失败闭门，审计失败高声告警但不阻断已授权操作**"。一句话抓住了 T-013 在第十一节安全基线下的取舍核心。返回 500 反而会引发"管理员反复重试 → 重复创建"的二次伤害，反例给得到位。
8. ✅ **`Actor` + `ActorResolver` DI 模式** — `internal/audit` 不直接解析 token、不导入 auth/admin 包；T-005b 接入只换 resolver，recorder/store/migration 零改动。Phase 2-B 起跳零摩擦。
9. ✅ **expvar 而非 Prometheus** — `omt_audit_record_failures_total` 走标准库 expvar，避免新增依赖触发许可证 propose，符合第八节技术栈纪律 + 名字保持 Prometheus 风格便于未来一行迁移。
10. ✅ **测试方案双轨** — 默认 `go test ./...` 用 fake SQL driver（无 Docker / 无网络），真 PG 用 `//go:build e2e` + `OMNITOKEN_TEST_DATABASE_URL` 环境门控。既满足第十节"禁 mock DB for integration"又不让本地开发卡 Docker，最优解。

### Questions（实施时必须回应）

**Q-1 (MEDIUM)** — `down.sql` 必须**精确**反向 rename，否则 `000002_indexes.up.sql:10` 的 `audit_logs_created_at_idx` 在回滚时会找不到 `audit_logs` 表（或找到了表但列名错乱）。建议 `down.sql` 严格按 up 的逆序：先 DROP 新索引 → 反向 rename 列 → DROP 新增列。实施时跑一次 `migrate up && migrate down && migrate up` 三联确认幂等。

**Q-2 (LOW)** — 测试 case #8："失败业务请求仍可写一条 `status_code >= 400` 的审计尝试，若 `action` 缺失则使用 `unknown_admin_write`"。请显式确认这条边界覆盖哪些情况：
- 401 鉴权失败 → 在 `auditMiddleware` 之前被 `adminAuthMiddleware` 拦截，**不进 audit**（设计上由"未授权"日志另行处理）
- 400 invalid body / 422 / 500 from handler → **进 audit**，`action` 可能未设（handler 未执行到 SetAction），fallback 为 `unknown_admin_write`
- panic recovery → 进 audit 还是仅 metric？建议进 audit + `action=panic_recovered`

请在 Result 注释里点名这条边界的覆盖。

**Q-3 (LOW)** — Import 边界。`internal/audit` 不能 import `cmd/admin`（cycle）也不能 import `internal/auth`（避免 Phase 2-B 时层级反转）。请确认 `Actor` / `ActorResolver` / `Recorder` 全部定义在 `internal/audit`，`cmd/admin` 反向 import 注入 `BootstrapActorResolver`。`go list -deps ./internal/audit` 实施后跑一次确认无回环。

### Nits（不阻塞）

**N-1** — `omt_audit_record_failures_total` 是单调计数器，请在变量定义处加一行注释"monotonic counter, never decremented"，便于未来迁移 Prometheus 时直接套 `counter_total` 语义。

**N-2** — `RENAME COLUMN before_state TO "before"` 写到 SQL 文件里时，golang-migrate 的 SQL splitter 对双引号嵌套不一定友好。建议在该 migration 单独的 SQL 文件内尾部不要混 `--` 注释，避免 splitter 卡尾。实施完跑 `migrate up` 验证。

**N-3** — 索引名 `audit_logs_actor_created_at_idx` / `audit_logs_resource_created_at_idx` 比项目里其他索引名（`audit_logs_created_at_idx`）长。bikeshed-级，可保留，也可短到 `audit_logs_actor_idx` / `audit_logs_resource_idx`。不阻塞。

**N-4** — 当前 `ip inet` 来自 `RemoteAddr`。生产部署在 reverse proxy / LB 后面时，`RemoteAddr` 是 LB IP，不是真客户端 IP。本任务不必处理 `X-Forwarded-For` / `X-Real-IP`（trust boundary 由部署决定），但请在代码 comment 留一行"deployment behind LB: switch to forwarded headers in Phase 3 deployment runbook"作为 future hint。

### 接受标准（PROPOSAL 层面，全部 ✅）

| 决策点 | 状态 |
|------|------|
| 1. 字段定义 + 类型（JSONB + 双引号 + CHECK） | ✅ |
| 2. 索引设计（白名单 2 条 + 黑名单 5 条 + 理由） | ✅ |
| 3. before 状态获取（推荐 A + 取舍说明） | ✅ |
| 4. 失败容忍策略（WARN + metric + 不阻断） | ✅ |
| 5. actor_id 占位 + 未来接入（ActorResolver DI） | ✅ |
| 6. 测试方案（8 case + 双轨 + ≥85%） | ✅ |

**Codex 可以开始 T-013 实施 commit。请在实施 commit 的 Result 注释里逐条回答 Q-1 / Q-2 / Q-3。N-1~N-4 视情况采纳，不强求。**

---

## R-013 (T-013, commit `d5b379d`)

**结论: `[+] Approved`** — 0C / 0H / 0M / 3N。`internal/audit` 95.9%（>85% 目标），39 测试用例。Pre-prop 的 N-1（counter 注释）/N-4（behind-LB 注释）已采纳。

**正面信号**:
1. ✅ migration 000007 严格按 PROPOSAL：rename 4 列 + add 3 列 + 3 CHECK + 2 索引；down 反向序对称。Q-1 三联 `up→down→up` Codex 已验证。
2. ✅ Q-2 边界对齐：panic recovery 写 `action=panic_recovered` + `status_code=500` 后 re-panic；401 在 `auditMiddleware` 之前被 `adminAuthMiddleware` 拦截，不进 audit。
3. ✅ Q-3 import 边界干净：`internal/audit` 零依赖 `cmd/admin` / `internal/auth`；`ActorResolver` 反向注入实现 DI。
4. ✅ `nullableJSON` 严格区分 SQL `NULL`（nil 快照）与 `{}`（空对象），保留 PROPOSAL §1 语义；dev-vkey 审计 after 只写 `api_key_id` / `organization_id` / `user_id` / `key_prefix`，零 secret 泄露。
5. ✅ `Scope` mutex 保护并发 `SetAction` / `SetBefore` 等；goroutine + 2s timeout + `context.Background()` 与 `internal/usage` 工程模式完全对齐。

**N-1**: `cmd/admin/main.go:207` `newMux(... , auditRecorders ...audit.Recorder)` 变长参数纯为测试 ergonomic 服务。`main()` 正确传入；但测试侧"忘传"会静默 no-op。建议改为必传参数 + 测试侧用 `audit.NewRecorder(nil, logger)` 显式 no-op。

**N-2**: `recorder.go:56-59` `statusCode == 0 → 200` fallback 依赖 `httpx.StatusRecorder` 在未 `WriteHeader` 时返回 0。若该 helper 行为变更（例如默认返回 200）则此分支变死代码。加一行注释或一条断言测试锁住契约。

**N-3**: `internal/audit/README.md` 仅 1 行。补一段"用法 / 接口边界 / 不做什么"，与 `internal/usage` / `internal/auth` 的 README 风格对齐。

**Phase 2-A 进度: 1/2** — T-014 可起。

---

## R-014-prop (T-014 告警段 PROPOSAL, commit `e2a9d98`)

**结论: `[+] Approved`** — 4 点决策都给得干净，可进入告警段实施。

**正面信号**:
1. ✅ 信号源选定时扫 `usage_events`，并明确切断了"middleware 入账时直接计数"会带来的热路径污染 / 并发 / 多实例分片复杂度。
2. ✅ 内存 map 去抖 + 单 admin 实例假设显式声明；多实例 + Redis 留 Phase 3，不在本任务偷偷扩范围。
3. ✅ 阈值 `OMNITOKEN_ADMIN_KEY_ANOMALY_RPM_5M`，非正数 / 非法值回退默认 + 一条 WARN——配置错误不静默吞掉。
4. ✅ WARN 白名单严格 4 字段（`api_key_prefix / window_start / count / threshold`），显式禁列 `api_key_id / user_id / organization_id / prompt / Authorization`，对齐 §11.6。
5. ✅ 明确 "5min 滞后" 不是熔断，未来 T-015 RPM 硬拦截走请求路径而非复用此机制——边界清晰，避免后续误用。

**N-1**: map 清理早于当前窗口的历史项时，建议在测试里覆盖"上一窗口没触发告警的 key 也要被清掉"的 case，避免内存泄漏隐患。
**N-2**: WARN payload 里 `window_start` 用 RFC3339 UTC 字符串，方便 grep + log 聚合工具切片。Codex 实施时显式选这个格式。

---

## R-014a (T-014 audit 查询 API + 前端实施, commit `ff2b7a6`)

**结论: `[+] Approved (partial)`** — 0C / 0H / 0M / 3N。仅覆盖 audit 查询 API + 前端 Audit tab；告警段待 R-014-prop 已 approve 后再实施 + 单独 review。`cmd/admin` 71.4%（不下降）。

**正面信号**:
1. ✅ `overviewStore.LoadAuditLogs` 接口扩展沿用 T-009a 模式；`adminAuditLog` JSON 形状与 T-013 audit_logs schema 字段一一对应。
2. ✅ `buildAuditLogsQuery` 参数化 SQL + 动态 WHERE 拼接，`fmt.Sprintf("$%d", ...)` 仅用于占位符序号，filter 值全走 `args`——无 SQL 注入面。
3. ✅ Limit 双层防御：parse 阶段 ≤500，`LoadAuditLogs` 入口再 clamp 一次；`resource_id / ip` nullable 走 `*string` 序列化为 JSON `null`，与 SQL `NULL` 语义一致。
4. ✅ `[]adminAuditLog{}` 初始化而非 `nil`——空结果 JSON 出 `[]` 不出 `null`，前端零特例。DB 未配置走 200 + 空数组，与 overview/users/models 降级一致。
5. ✅ 前端 vanilla JS 沿用 `users.js` 风格，`escapeHTML` 防 XSS、`<pre>` 包 `JSON.stringify(before/after, null, 2)`、点击行展开/收起的 `expandedRow` 单值状态足够简单；`node --test` fetch mock 覆盖 loading+empty / error 两个测试函数（三态都摸到）。

**N-1**: `"before"::text` / `"after"::text` 强制把 JSONB 反序列化为 text 再 `json.RawMessage` rehydrate。这依赖 INSERT 侧（T-013 `$N::jsonb`）已经 validate 过，所以扫描端不再校验是安全的——但 SQL 上方加一行注释说明这条 invariant，能让未来改 schema 的人不踩坑。
**N-2**: `audit.test.js` 只有 2 个 `test(...)` 函数（合并 loading+empty 一组、error 一组）。T-014 接受标准 ③ 写的是"三态"——实质覆盖了但语义可读性差，未来加一条独立"loading 中"断言（fetch 不 resolve 的中间帧）能让回归更稳。不阻塞。
**N-3**: `audit.js` 内 `expandedRow = "${entry.request_id || "audit"}-${index}"`——空 request_id 时所有行的 rowKey 形如 `audit-0/audit-1`，仍唯一，OK；但生产环境 audit_logs 全部有 request_id（T-013 落 `httpx.RequestIDFromContext`），此 fallback 几乎死代码。可保留作防御。

**Phase 2-A 进度: 1.5/2** — 告警段待 Codex 按 PROPOSAL 实施。

---

## R-014b (T-014 告警段, commit `db425c7`)

**结论: `[+] Approved`** — 0C / 0H / 0M / 2N。`internal/usage/anomaly` 82.1%（>80% 目标），cmd/admin 72.0%（不下降）。R-014-prop 的 N-1 / N-2 全部采纳。

**正面信号**:
1. ✅ `Monitor` 状态机干净：`markSeen` 为亚阈值入 map（值 false），`markAlerted` 为达阈值首次告警后翻 true 并返回旧值——debounce 语义无歧义；`stateKey{apiKeyID, windowStart}` 复合键天然支持跨窗口独立计算。
2. ✅ `cleanupBefore(windowStart)` 在每轮 `Scan` 入口先跑，移除所有 `windowStart < current` 的 entry——包括 R-014-prop N-1 提的"未触发告警但累过的 key"，map 不会泄漏。
3. ✅ 生命周期统一在 `runCtx, stopBackground := context.WithCancel(...)`，monitor 与 `httpx.Run` 共享同一 ctx；server 退出时 monitor 自动停。`db == nil` 时 `startKeyAnomalyMonitor` 早 return，与 audit / overview 降级模式一致。
4. ✅ env 解析三态：空→默认 100、非法值→WARN log + 默认；不静默吞错。WARN 字段严格 4 件（`api_key_prefix / window_start RFC3339 UTC / count / threshold`），`defaultString(prefix, "unknown")` 兜底防 `ak.key_prefix IS NULL` 时 log 空字段。
5. ✅ SQL: `LEFT JOIN api_keys + WHERE api_key_id IS NOT NULL + GROUP BY` 边界清晰；窗口参数化、`Truncate(time.Second)` 抹掉亚秒抖动，使两轮 `Scan` 的 windowStart 严格不重叠。

**N-1**: `monitor.go:90` `run(ctx)` 用 `time.NewTicker(interval)` 首次触发要等 5min。生产可接受但开发/演示时观感慢。可考虑入口先 `m.Scan(ctx)` 跑一次（与定时不冲突，cleanup 已就位）。
**N-2**: `Scan` 函数为方便测试导出，扩大了 API surface。建议在 README 里点名"导出仅为 test/manual trigger，正常路径走 `Start`"，避免后续误用为同步告警入口。

**Phase 2-A 进度: 2/2 ✅** — T-014 完成。Phase 2-B 起点 T-005a 可起。

---

## R-005a-prop (T-005a PROPOSAL, commit `519086e`)

**结论: `[+] Approved`** — 4 点全清，可直接进实施。

**正面信号**:
1. ✅ §1 硬编码 Go map 决策给的理由完整：<20 actions / schema invariant / DB 表方案隐含 CRUD+审计+缓存+invalidation+回滚一整套成本。同时保留 `Engine.Authorize` 形状稳定，Phase 3 自定义角色时换内部 policy source 即可——稳。
2. ✅ §2 action vocabulary 与 `audit_logs.resource_type` 对照表完整：8 个 action × 3 role 真值表锁死；`unknown_admin_write` 显式排除避免循环；T-016 provider CRUD 留待对应任务卡命名，不抢未来词表。
3. ✅ §3 不缓存的论证扎实：低 QPS 可承受 + 撤销/禁用立即生效 + 没有 cache invalidation hook。未来 cache 接入路径（在 T-005b 后包装 `RoleCache`）已留好，不在第一版背技术债。
4. ✅ §4 8 个 reason 常量分层合理：`allowed_by_admin/viewer/member` 三态独立，便于 T-005b 落 audit `after` 时区分"谁批准"；unknown action 归 `action_not_permitted` 不暴露 action surface；DB 错误用 `%w` 包装为 `role_lookup_failed`。
5. ✅ 测试矩阵明确（fake store 三角色 × 全 action + unknown × 2 + disabled + multi-role + DB error + nil engine），Postgres store 用现有 fake SQL driver 模式断言 JOIN——不引 testcontainers，与 T-013/T-014 测试纪律一致。

**N-1**: §3 多角色 fall-through 语义建议在实施时显式注释——当前矩阵下 `admin > viewer > member` 的 fall-through 路径在三角色 superset 关系里其实不可达，但实现要保证"highest 角色 deny 后继续查下一角色"的 OR 语义，避免未来加 action 时出现"admin 没批但 viewer 能批"的反常行为时被默默吞掉。建议在测试里加一条"admin 没权限但 viewer 有权限"的合成 action 用例锁住语义。
**N-2**: 后续 T-014 audit 查询 API 没有 `reason` filter，但 R-005a-prop §4 的 reason 字段进入 audit `after` 后会成为运维高频检索维度。建议 T-005b 实施时顺手让 audit handler 多接一个 `reason=` filter（不强求 T-005a 改 audit handler，记账即可）。

**Phase 2-B 进度: 0/3** — Codex 可开 T-005a 实施 commit。

---

## R-005a (T-005a, commit `89ef188`)

**结论: `[+] Approved`** — 0C / 0H / 0M / 2N。`internal/rbac` 89.9%（>85% 目标）。N-1 fall-through 测试已锁。

**正面信号**:
1. ✅ `Engine.Authorize` 评估顺序: invalid actor → empty action → nil engine → store lookup → not-found → disabled → policy match。每一档独立 reason，短路语义清晰；`e == nil` 检查与 `e.store == nil` 短路 OR 配合，nil-receiver 调用不 panic。
2. ✅ 多角色 OR 语义实现严格：`rolePriority=[admin,viewer,member]` 顺序遍历，首个 allow 的角色返回；`engine.go:56-58` 显式注释 fall-through 不能吞底层角色——`TestAuthorizeMultiRoleFallsThroughDeniedHigherRole` 用合成 `synthetic_viewer_only` action 锁住语义，admin deny / viewer allow / user 双角色 → `allowed_by_viewer`。
3. ✅ 矩阵 100% 对齐 PROPOSAL §2 表格：admin 8 个全 true / viewer 4 view + view_own_usage false / member 仅 view_own_usage。`policy.allows` 用 `ok && allowed` 双校验，缺失 key 与显式 false 行为一致。
4. ✅ `NewEngine` / `NewPostgresStore` 双层 nil 防御：`isNilStore` 用 reflect 处理 typed-nil interface 情况（`NewEngine(NewPostgresStore(nil))` 返回 nil 而非含 nil store 的 Engine）；引擎不擅自 allow，对应"DB 未配置调用方降级"约束。
5. ✅ SQL `users LEFT JOIN role_assignments LEFT JOIN roles ORDER BY canonical_name` — `Found=true + 空 Roles` 与 `Found=false` 各自独立可观测；`status / role` 都用 `sql.NullString` 兜底；测试用 fake SQL driver 断言参数与 query 文本双重验证。

**N-1**: `Authorize` 顺序中 `lookup.Status != StatusActive` 在 `len(Roles) == 0` 之后，所以"disabled 用户无角色"会返回 `role_not_found` 而非 `user_disabled`。两种 reason 都是合理 deny，但 audit 视角下"user_disabled"信息密度更高。建议把 status 检查上移到 `!lookup.Found` 之后、`len(Roles) == 0` 之前。非阻塞，T-005b 接 audit `after` 字段时一并调整即可。
**N-2**: `AllActions` 导出切片便于 T-014/T-005b 后续做"展示当前用户能做什么"探针端点；但当前没人引用，IDE 会标 unused。保留作 vocabulary 单一信息源，OK。

**Phase 2-B 进度: 1/3** — T-005a 完成。T-015（budget 扣费拦截）与 T-005b（router 接入）可起；两者可一定程度并行。

---

## R-015-prop (T-015 PROPOSAL, commit `7462418`)

**结论: `[+] Approved`** — 4 点全清，可进实施。N-1/N-2 落实施时一并带上。

**正面信号**:
1. ✅ §1 USD cents 选型三条理由扎实：admin 已展示 `estimated_cost_usd` / token 数在多模型下不稳定 / `bigint cents` 比浮点 numeric 在表单/JSON/测试中更稳；显式不复用 `api_keys.monthly_budget_usd`，把 budget 落在 user 维度对齐产品语义（管理员给员工划线）。
2. ✅ §1 比较与展示分离的细节漂亮：比较用 `SUM(cost_usd) >= budget_cents::numeric / 100` 走 exact numeric，展示用 `CEIL(SUM(cost_usd) * 100)::bigint` 防 sub-cent 低估；同时显式保留 `used_tokens` + 兼容别名 `quota`，前端不破。
3. ✅ §2 检查时机定在"虚拟 Key 鉴权后、调用上游前"是正确切点；显式说明"当前请求可能把用户从 99.9% 推到 100%+，下一次拦"作为 v1 已知 tradeoff（T-INT 记入已知限制）——不假装零误差，也不为它引 Redis。
4. ✅ §3 402 envelope 严守 §11.6：error body 不含 budget/used/user_id/key prefix，日志可记低敏字段 + request_id；DB 异常 `fail-closed → 500 quota_check_failed` 阻止"沉默烧钱"，与"安全失效闭门"口径一致。
5. ✅ §4 前端 inline 编辑 + 角色显隐的可注入 role provider 设计——后端 GET 返回**统一字段形状**（不按角色裁字段），只在写动作处 gate；T-005b 接 `/api/admin/me` 时只换 provider，不动 Users tab 渲染逻辑。这是 T-015 / T-005b 解耦的关键。

**N-1**: §1 顺手提到 `api_keys.monthly_budget_usd` 是 key 级遗留字段。建议在实施 commit 的 Result 里点名"该字段当前 Phase 2 无人引用"（grep 确认），避免 vNext 做 per-key rate limit 时与 user budget 混淆；也可一并记入 v1 已知遗留。
**N-2**: §1 CEIL 展示 vs exact 比较的语义差（"UI 显示 100% 但仍可调用直到实际成本 ≥ 0.37"）建议在 admin Users tab 加一行 tooltip 或 README 注释，免得运维误判"为什么显示满了还在烧"。
**N-3**: §2 没有点名 budget checker 在 `cmd/gateway` 的具体接入位置（chi middleware 链中 auth 之后、reverse proxy 之前的哪个 slot）。实施时按现有 `internal/proxy` middleware 顺序自然插入即可，不需要 propose 再来一轮。

**Phase 2-B 进度: 1/3** — Codex 可开 T-015 实施。

---

## R-015 (T-015 实施, commit `0041c11`)

**结论: `[+] Approved`** — 0C / 0H / 0M / 3N。quota 8 测试，admin 3 新测，前端 2 测。提交时 `tmp-go-cache/` 已清，`api_keys.monthly_budget_usd` grep 确认无业务代码引用（迁移脚本/文档以外）。

**正面信号**:
1. ✅ Migration 000008 极简（一条 `ALTER TABLE ... ADD CHECK`），down 对称。`monthly_budget_status` SQL 内嵌注释明确"display CEIL / enforcement exact numeric"——R-015-prop N-2 落实施侧。
2. ✅ `Checker.Check` 严格按 PROPOSAL §2：subject 空 / store nil / DB error 三档独立 reason；`MonthWindow` UTC 月对齐；DB 错误 fail-closed 不静默放行。
3. ✅ gateway `enforceMonthlyBudget` 插在 `protectGatewayRoute`（auth）之后、chat handler 之前，与 R-015-prop N-3 建议的接入位置一致；checker 为 nil 时 short-circuit pass-through（DB 未配置降级）。
4. ✅ 402 envelope 严格 `type=quota_exceeded, code=monthly_budget_exhausted`，error body 不含 budget/used/key prefix；WARN log 仅含 request_id / org_id / user_id / used_cents / budget_cents——对齐 §11.6 安全基线。
5. ✅ admin `PATCH /api/admin/users/{id}/quota` 走 `protectedWrite`（admin auth + audit middleware）；`audit.SetAction(rbac.ActionUpdateQuota)` 直接复用 T-005a vocabulary 常量；before/after 只装 `{budget_cents}`，零额外字段；前端 `getRole()` 可注入设计为 T-005b 接 `/api/admin/me` 留接口零摩擦。

**N-1 (housekeeping)**: 工作树有 `tmp-go-cache/` 未跟踪目录（Codex 本地 GOCACHE）。已添加到 `.gitignore`，Codex 下次跑测试前 `rm -rf tmp-go-cache/` 即可。
**N-2 (testing)**: R-015-prop N-1 提到要 grep 确认 `api_keys.monthly_budget_usd` 当前无人引用——请 Codex 在 Result 里加一行 grep 结果（应该是 `grep: no matches`，表示该旧字段确实是孤儿）。
**N-3 (frontend)**: 前端 `getRole()` 默认 `"admin"`，没有 `?role=viewer` 的 URL 解析。联调时若要点 viewer 模式，得手改 `web/src/app.js:8` 的 `createUsersView(api)` 为 `createUsersView(api, {role: "viewer"})`。本任务接受标准只要求"viewer 不显示编辑"，前端测试已覆盖（`users view hides quota editing for viewer role`），不阻塞；T-005b 接 `/api/admin/me` 时自然解决。

**Phase 2-B 进度: 2/3** — T-005b 可起。

---

## R-017a-prop (T-017a PROPOSAL, commit `docs: propose T-017a virtual model resolver`)

**结论: `[+] Approved`** — 3 点决策全采纳建议方向 + 6 case 测试矩阵。可直接进实施。

**正面信号**:
1. ✅ §1 单列 `real_model text` v1 落地 + T-017b 增列 `fallback_models text[]` 不破坏现有 schema，是最干净的向后兼容路径。
2. ✅ §2 未命中透传的"后向兼容"理由扎实——现有直传 `ep-xxx` 真实模型名的客户端不被打断；只对 `disabled` 明确报错避免歧义。
3. ✅ §3 重写点在 `cmd/gateway/virtualModelMiddleware` 而非 `internal/proxy/Director`——保 proxy 协议无感、把虚拟模型语义彻底锁在 gateway 层。`VirtualModelKey` 通过 ctx 传给 usage middleware 让 `model_requested` 落对值。
4. ✅ §4 6 case 测试矩阵覆盖 resolver 命中/未命中/disabled/DB error + gateway 集成（body 重写 + ctx 注入）+ 真实模型透传。

**N-1**: 实施时 `virtual_models` seed 写入 `deploy/postgres/002_seed.sql` 务必把 5 条映射加进去（任务卡接受标准已点名），别只跑 migration 留空表。
**N-2**: §3 提到"重新构造 Body 交给反代层"——Go 的 `http.Request.Body` 是 io.ReadCloser，重写时记得 `bytes.NewReader(...) + req.ContentLength` 一起改，否则上游会读到错误的 Content-Length。

---

## R-005b-prop (T-005b PROPOSAL, commit `docs: propose T-005b admin auth login`)

**结论: `[+] Approved`** — 4 项补充全部落到位（JWT 措辞剔除 / trade-offs / seed 密码 / XSS）+ 6 case 测试矩阵。可直接进实施。

**正面信号**:
1. ✅ §1 "server-side in-memory session, token 不透明随机字符串"措辞精准，避免后人误读为 JWT 状态。32-byte 随机 hex 复用现有 `adminAuthMiddleware` 的 Bearer 解析，集成成本最小。
2. ✅ §3 两条 trade-off 显式声明（重启清空 / 多实例不同步），且明确"需要写进部署文档"——避免运维踩坑。
3. ✅ §4 seed bcrypt hash 方案 a 选得对：本地联调 + CI 一条命令起容器即可登录；生产用 `cmd/admin-cli` 留 vNext，没有提前预言式建抽象。
4. ✅ §5 localStorage XSS 风险显式认账 + "v2 接收用户输入内容时改 HTTP-only cookie" 留好未来切换钩——v1 不引 CSRF/cookie 复杂度。
5. ✅ §6 登录端点自审计 `actor_type=anonymous` + email + reason，撞库行为可追溯，与 T-013 audit middleware 模式一致；§7 6 case 覆盖完整。

**N-1**: §4 seed bcrypt hash 务必**预先在本地 `bcrypt.GenerateFromPassword([]byte("admin-dev-password"), 10)` 生成一次**，把结果硬编码进 SQL；不要在 seed 运行时 hash（seed 容器是裸 psql，没有 bcrypt 工具）。Result 里贴出生成命令 + hash 值便于复现。
**N-2**: §3 24h 过期建议在代码里用常量 `defaultSessionTTL = 24 * time.Hour`，不要散落字面量——v2 调短/调长时一处改完。
**N-3**: §7 "revoked token" case 需要 `/api/admin/logout` 实现 server-side 失效（从 in-memory map 删 key）。任务卡里已含 logout 端点，实施时确保两者绑定。

**Phase 2-B 进度: 2/5** — Codex 现可并行实施 T-017a + T-005b（T-015 已 ✅）。

---

## R-017a (T-017a 实施, commit `0021e37`)

**结论: `[+] Approved`** — 0C / 0H / 0M / 2N。router 5 测试 + gateway 2 测试，virtualModel ctx 注入 + usage middleware 联动到位。

**正面信号**:
1. ✅ Middleware 链 `protectGatewayRoute → enforceMonthlyBudget → resolveVirtualModel → chatHandler` 顺序正确——auth/quota 都在路由解析前，符合 PROPOSAL §3。
2. ✅ Body 重写严格：`ReadAll → decode → modify → marshal`，**同步设置 `ContentLength` 和 `Content-Length` header**（R-017a-prop N-2 已采纳）；`r.Body` 用 `NopCloser(bytes.NewReader(body))` 重新挂回，反代不会读半截。
3. ✅ 容错路径完整：`r.Body == nil` / decode 失败 / `model` 字段不是非空字符串 / 未命中 → 都走 pass-through 不报错；只有命中但 `disabled` 返 400 `virtual_model_disabled`。
4. ✅ `httpx.WithVirtualModel(ctx, modelRequested)` 注入虚拟原名；`internal/usage/middleware.go:108-114` 读 `VirtualModelFromContext` 优先于 body 中的 model，让 `usage_events.model_requested` 落对值。
5. ✅ Seed 5 条映射全部进 `002_seed.sql` 且 `ON CONFLICT (name) DO UPDATE`——重复 seed 不会炸。

**N-1**: `payload` 是 `map[string]any`，重新 `json.Marshal` 后 key 顺序非确定（Go map iteration）。功能上 JSON 顺序无关，但若未来 audit/比对环节做 body hash，结果会和客户端原 body 不一致。建议在代码注释里点名"key order may differ after rewrite; do not hash request body for signature"。
**N-2**: `payload["model"] = res.RealModel` 后 `json.Marshal(payload)` 失败时，代码 `if err == nil` 静默吞掉错——`body` 保留原内容（仍是虚拟名），但 ctx 已设置虚拟名 → 上游收到虚拟名 + usage 记录虚拟名（两者一致但都是错的）。极不可能触发，但应至少 `logger.Warn`。

---

## R-005b (T-005b 实施, commit `62504b9`)

**结论: `[!] Changes Requested`** — **3 HIGH / 1 MEDIUM / 2 NIT**。不能合，请按 H-1/H-2/H-3 修后再 review。

### HIGH (必须修)

**H-1**: 登录端点**完全未走 audit middleware**。`mux.Handle("POST /api/admin/login", http.HandlerFunc(makeLoginHandler(...)))` 是裸 handler，没有 `adminAudit` 包装。Handler 内部调 `audit.SetAction("login_failed")` / `audit.SetAfter(...)`——但 `audit.SetAction` 只在 ctx 里有 `Scope` 时才生效，**裸 handler ctx 没 Scope = 全部静默 no-op**。撞库 / 错密码 / 成功登录全都不落 audit_logs。PROPOSAL §6 "登录端点是否自审计：推荐落库 ... 用 `actor_type=anonymous`" **承诺未兑现**。
- 修法：建一个 `adminAuditOnly` middleware（只包 audit，不包 auth），把 login 端点用它包起来。或者直接在 login handler 内 `audit.NewRecorder(...).Record(ctx, entry)` 同步写——但要先解决 H-2。

**H-2**: `audit_logs.actor_type` 的 CHECK constraint 是 `IN ('bootstrap', 'user', 'system')`（T-013 migration 000007）。PROPOSAL §6 说 `actor_type="anonymous"`，但 **DB 层会拒绝插入**。这是把 H-1 修了之后会立即暴露的二级故障。
- 修法二选一：① 加一个 migration `000011` 放宽 CHECK 到 `('bootstrap','user','system','anonymous')`；② PROPOSAL §6 改用 `actor_type='system'` + `action='login_attempt'` + `after={email, status}`——语义稍弱但不动 schema。倾向 ①，因为 anonymous 是真实场景。

**H-3**: 测试覆盖**漏 5 / 6**。PROPOSAL §7 列了 6 case，实际只有 `internal/auth/admin_session_test.go:TestSessionStore` 一个测试，覆盖的是 SessionStore 数据结构。**0 个 login flow 测试**：
- ❌ login success → 返 token + 校验
- ❌ wrong password → 401 `invalid_credentials`
- ❌ disabled user → 403 `user_disabled`
- ❌ expired token → 401（需要时间注入）
- ❌ revoked token (logout 后老 token 不能用) → 401
- ❌ audit on login attempt（H-1 解决后）

R-005b-prop §7 我点名要求"与 T-005a / T-015 保持同档"，admin_session.go 单测远低于这条线。

### MEDIUM

**M-1**: `store.Authenticate` 用字符串比较错误：`if err.Error() == "invalid credentials"` / `else if err.Error() == "user disabled"`。Stringly-typed control flow，任何 error 文案变动会静默改成 500 路径。应定义 `var ErrInvalidCredentials = errors.New("invalid credentials")` 等 sentinel，用 `errors.Is` 比较。

### NIT

**N-1**: `adminAuthMiddleware` 改名为 `adminSessionMiddleware` / `adminAuthBootstrap` + `authChain` 组合。设计 OK，但实施时 `authChain(adminAuthSession, adminAuthBootstrap)` 是"session 优先，bootstrap 兜底"——这意味着即使新登录态可用，旧 `OMNITOKEN_ADMIN_BOOTSTRAP_TOKEN` 仍永久有效。R-005b-prop 没明确禁掉它，但 T-INT 联调时应在 `.env.example` 里把 bootstrap 默认值留空 + README 写"production 部署关掉 bootstrap"。
**N-2**: bcrypt seed hash 已嵌入 `002_seed.sql`，但 Result 注释没贴出生成命令（R-005b-prop N-1 要求）。请在 T-005b 任务卡 Result 区补一行 `Generated via: htpasswd -bnBC 10 "" admin-dev-password | cut -d: -f2-` 或 Go snippet，便于复现。

### 接受标准复盘

| 验收门 | 状态 |
|---|---|
| 登录端点 audit 落库 | ❌ H-1 |
| actor_type anonymous 可用 | ❌ H-2 |
| 6 case 测试覆盖 | ❌ H-3 |
| bcrypt cost 10 | ✅ |
| session 24h TTL + Revoke | ✅ |
| RBAC 落 admin 写路由 + 真实 actor_id 切换 | ✅ |
| 前端 login + 401 跳转 | ✅ |

**Phase 2-B 进度: 3/5**（T-017a ✅；T-005b 待修）。修完后单独一个 commit `fix: harden admin login audit & tests`，don't amend `62504b9`。

---

## R-005b-fix (T-005b 修复, commit `670dc60`)

**结论: `[+] Resolved`** — 3 HIGH + 1 MEDIUM 全部修复，1 个未声明的行为变更需补一行文档（NIT，不阻塞）。

**Resolved 列表**:
- **H-1 ✅** `adminAuditOnly` 抽出来包 login handler，audit middleware 真正生效；`TestAdminLoginAuditLogs` 端到端验证 audit recorder 收到 entry 且 `after.user_id` 正确。
- **H-2 ✅** migration `000011_allow_anonymous_audit` 用 DROP + ADD CONSTRAINT 模式（不能 ALTER 现有 CHECK），新 set 含 `anonymous`；down 严格反向。
- **H-3 ✅** 6 个新测试：`TestAdminLoginSuccess` / `TestAdminLoginWrongPwd` / `TestAdminLoginDisabled` / `TestAdminSessionExpired` / `TestAdminSessionRevoked` / `TestAdminLoginAuditLogs` 全部就位，对应 PROPOSAL §7 6 case。
- **M-1 ✅** sentinel `ErrInvalidCredentials` / `ErrUserDisabled` 定义在 store 层，handler 用 `errors.Is` 比较——control flow 不再依赖字符串。

**N-1 (未声明的行为变更)**: Codex 把"`bootstrapToken == ""` → 503 `admin_auth_not_configured`"塌成"`bootstrapToken == "" || invalid → 401 unauthorized`"，并在 commit message 里称之为"Bug 修复"。这其实是 **T-010 R-010 §3 显式批准的设计**：未配置 token 走 503 是"安全失效闭门"的信号。
但在 T-005b 新架构下（session 主、bootstrap 兜底）这个行为变更**实际上合理**——session 是 primary path，bootstrap 空 = 普通的 fallback-disabled，不再是 "admin 没配置"。所以变更可保留，但请：
1. 在 commit message 或 follow-up commit 里改措辞：不是"Bug 修复"，是"adapt 503→401 for session-primary architecture"
2. 在 `cmd/admin/main.go` `adminAuthMiddleware` 内加一行注释解释这个语义转变

不强求新 commit，下一次涉及该函数的改动顺手带上即可。

**Phase 2-B 进度: 4/5 ✅** — T-017a + T-005b 全绿；T-INT 解锁。

---
