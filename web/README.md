# Web

Static admin console for OmniToken. It mirrors the visual language of `../测试前端.html`, but production code lives here and reads real admin APIs only.

Open `index.html` directly or serve `web/` with any static file server. Pass `?admin=http://localhost:8081` to point the console at a running admin API.
